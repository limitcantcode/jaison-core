components:
  # Must set one of 'directory' or 'endpoint'
  - directory: "path/to/project/root/" # For windows, replace each \ with \\
  - endpoint: "127.0.0.1:50000" # If you set this URI, you will need to run the component separately before running this